CREATE TABLE PRODUTOS(
  IDPRODUTO INT PRIMARY KEY IDENTITY,
  NOME VARCHAR (50) NOT NULL,
  DESCRICAO VARCHAR(50) NOT NULL,
  VALOR MONEY NOT NULL,
  FK_CATEGORIAS INT FOREIGN KEY REFERENCES CATEGORIAS(IDCATEGORIA)
  )

  CREATE TABLE CATEGORIAS(
  IDCATEGORIA INT PRIMARY KEY IDENTITY,
  NOME VARCHAR (50) NOT NULL
  )


  DROP TABLE PRODUTOS

  INSERT INTO CATEGORIAS (NOME)VALUES ('BRINQUEDO')
  INSERT INTO CATEGORIAS (NOME)VALUES ('ELETRODOMÉSTICO')


  SELECT * FROM CATEGORIAS

  SELECT * FROM PRODUTOS

  INSERT INTO PRODUTOS (NOME, DESCRICAO, VALOR, FK_CATEGORIAS)VALUES ('GELADEIRA', 'GELADEIRA PRETA DO AZAR', '1000,00', '2')

  INSERT INTO PRODUTOS (NOME, DESCRICAO, VALOR, FK_CATEGORIAS)VALUES ('TELEVISÃO', 'HD LED', '2000,00', '2')

  INSERT INTO PRODUTOS (NOME, DESCRICAO,